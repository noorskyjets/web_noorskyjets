# Free tier: https://web3forms.com — create a key linked to the inbox that should receive inquiries.
# Copy this file to `.env.local` and paste your key (never commit `.env.local`).
VITE_WEB3FORMS_ACCESS_KEY=
